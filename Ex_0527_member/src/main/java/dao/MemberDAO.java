package dao;

//DAO(DATA ACCESS OBJECT)
// DB의 데이터에 접근하기 위한 객체
// DB에 접근하기위해 로직을 분리하기 위해 사용한다.
// 직접 DB에 접근하여 DATA를 삽입,삭제,조회등 조작할수있는 기능을 수행한다 

public class MemberDAO {

	_single
	
	
}

	public List<MemberVO> selectList(){
		
		List<MemberVO> list = new ArrayList<>();
		Connection conn = null;
		Prepared
			}
	}
